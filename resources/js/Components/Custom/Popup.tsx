import React from "react";
import { Auth } from "@/Types/Auth";
type Props = {
    auth: Auth;
};

export default function Popup(props: Props) {
    return (
        <div>
            <h1>popup</h1>
        </div>
    );
}
